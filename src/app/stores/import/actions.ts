import { createAction } from '@ngrx/store';

export const importPhase1 = createAction('[Import] Phase 1');
export const importPhase2 = createAction('[Import] Phase 2');
export const importPhase3 = createAction('[Import] Phase 3');
